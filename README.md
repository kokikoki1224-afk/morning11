# 日本株モーニング11 自動化（STEP2まで）

「日本株モーニング11」（claude.aiのartifactで手動運用中のレポート）と同じスコアリング思想・5指標構成を、GitHub Actionsで毎朝自動実行するための土台。**現時点ではSTEP2（5指標の自動取得、うちドル円は基準時刻を再検証して修正済み）までを実装済み**。スコア計算・AI分析・HTML生成・GitHub Actions/Pagesは未実装（後続ステップ）。

既存のclaude.ai artifact・Claudeスケジュールタスク（`nihonkabu-morning11`）とは完全に独立しており、それらは変更していない。

## データソースと基準時刻（指標ごとに検証済み）

| 指標 | 採用ソース | 基準時刻・集計方式 | 検証結果 |
|---|---|---|---|
| Nasdaq総合 | yfinance `^IXIC`（日足） | NASDAQ立会時間（〜16:00 ET）の終値。単一セッションのため曖昧さなし | 株探の公表値と一致（2026-09-04: 26,506.99 / -0.29%） |
| SOX | yfinance `^SOX`（日足） | フィラデルフィア証取立会時間の終値 | 株探の公表値とほぼ一致（同日 +3.37%台） |
| KOSPI | yfinance `^KS11`（日足） | KRX立会時間（〜15:30 KST）の終値 | Seoul Economic Dailyの公表値と一致（同日 6,687.21 / +1.64%） |
| 米10年債利回り | FRED `DGS10`（プライマリ、**`FRED_API_KEY`未設定のため実動作は未検証**）／ yfinance `^TNX`（フォールバック） | 米国債券市場の引け値。時間足と日足を突き合わせ、日をまたいだ不整合が無いことを確認済み | 日足終値(4.784)と時間足最終値(4.784)が一致。問題なし |
| **ドル円（USD/JPY）** | yfinance `JPY=X`、**日足ではなく時間足(1h)からNY17:00(米東部時間)のスナップショットを抽出**（詳細下記） | NY17:00（外為市場で慣行的に使われる「日次の区切り」） | 下記「USD/JPYの基準時刻問題」参照 |

### USD/JPYの基準時刻問題（調査結果と対応）

**問題**：yfinanceの`JPY=X`日足（`interval="1d"`）は、24時間ほぼ連続で取引されるFXペアの集計区間があいまいで、実際に時間足と突き合わせたところ、日によっては前日比の符号（円高/円安）が逆転するほどの不整合を確認した。

検証例（2026-09-02〜09-03、時間足ベースで正しい値動きを追った場合）：
- 日足`Close`の系列：9/1=159.747 → 9/2=**160.196**（前日比+0.28%＝円安） → 9/3=158.923
- 同じ期間を時間足(1h)で丁寧に追うと、9/2の実際の値動きは159台からザラ場で158円台へ**下落**しており、日足の「9/2=160.196」という値は時系列上ありえない（前後のどの時間足バーにも該当する値がない）。yfinanceの日足エンドポイントが、24時間連続取引銘柄で内部的に取り違えている可能性が高い。

**対応**：日足を使わず、時間足(`interval="1h"`)を取得し、日本の為替報道が慣行的に「その日の終値」として扱う**NY17:00（米東部時間）**に最も近いバーの始値を「その日の代表値」として採用し、前日の同時刻との差分でスコア判定する。これにより時系列上の整合性を確認済み（例：9/2のNY17時値=158.674、9/3のNY17時値=155.779〜155.848）。

**残る限界**：日本の為替速報記事（外為どっとコム等）が引用する「終値」は、記事執筆時点のアドホックな時刻を指すことが多く、NY17:00ちょうどとは数十分〜1時間程度ズレることがある。今回の検証でも0.3〜0.5円程度の差が残った。これは「NY17:00」という一貫した基準を採用したことによる説明可能な差であり、日足エンドポイントの不整合（符号が逆転しうる）とは性質が異なるため許容する。

**運用上の注意**：本番（GitHub Actionsで日本時間朝に実行）では前営業日のNY17:00は確実に経過済みのため問題ないが、当日の途中経過を見るような使い方をすると直近日のスナップショットが未確定になる場合がある。

### 未検証・後続ステップ（STEP5）で確認予定

| 指標 | 候補ソース | 現時点の所見 |
|---|---|---|
| 日経225先物 | yfinance `NKD=F`（CME、近似値） | 日足の値動きが時系列上自然で、実際の大取夜間取引の清算値（9/5早朝 65,830円）ともほぼ一致（65,820円）。ただしCMEの営業日区切りとJST7:00の対応関係は未検証のため、USD/JPYと同様のスナップショット方式が必要か要確認 |
| TOPIX | yfinance `1306.T`（TOPIX連動ETF、代理指標） | `^TOPX`・`^TPX`は取得不可（データなし）だったため、ETF代理を採用。TSE立会時間（9:00-15:00 JST）内の単一セッションのため基準時刻の曖昧さは無い |
| 日本10年債利回り | 財務省公式CSV（`jgbcm_all.csv`） | 未着手。公式の単一日次値のため基準時刻の曖昧さは無い想定 |

## 不採用にしたソース
- **Stooq**：計画時点では無料・安定という評価だったが、実装して直接叩いたところ2026年9月時点でJavaScriptベースのbot対策チャレンジ（Cloudflare的な検証）が導入されており、単純なHTTPリクエストでは機械的に取得不能と判明。当面はyfinance一本に統一。
- yfinanceは非公式APIであり、レート制限や仕様変更が起こりうる（既知のリスク）。失敗時は「取得失敗」と明示してスコアから除外する方針を維持する。

## セットアップ

```bash
python -m venv .venv
source .venv/Scripts/activate   # Windows Git Bash
pip install -r requirements.txt
export FRED_API_KEY=xxxxx        # https://fredaccount.stlouisfed.org/apikeys で無料発行
python main.py --test
```

## ファイル構成（現状）
```
morning11/
├── main.py              # --test のみ対応（STEP2）
├── data/
│   └── market.py         # Nasdaq/SOX/US10Y/USDJPY/KOSPIの取得
├── config/
│   └── settings.json      # データソース定義・スコア閾値（score_threshold_percent, bond_yield_threshold_bp）
├── requirements.txt
└── README.md
```
`analysis/` `generator/` `templates/` `.github/workflows/` は後続ステップで実装予定のため現状空。

## GitHub Secretsに保存する予定の値（コードには絶対に書かない）
- `ANTHROPIC_API_KEY`（AI分析、STEP8で使用）
- `FRED_API_KEY`（米10年債データ、STEP2/3で使用）

## 未実装（後続ステップ・STEP3のスコア計算はUSD/JPYの基準確定待ちで保留中）
- STEP3: スコア計算（±0.2%ルール・bp判定・合計・総合判定）
- STEP4: 既存HTMLのプレースホルダ化・自動埋め込み
- STEP5: 日本市場（日経225先物・TOPIX・日本10年債）追加、NKD=Fの基準時刻検証
- STEP6以降: イベントカレンダー・ニュース・AI分析・GitHub Actions/Pages
